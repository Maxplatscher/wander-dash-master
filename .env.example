# Supabase
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Google Maps + Places (Autocomplete im Onboarding)
# In Google Cloud Console für denselben Key aktivieren:
# - Maps JavaScript API
# - Places API (Legacy)  ODER  Places API (New)
# Sicherheit: Key per HTTP-Referrer einschränken (z. B. http://localhost:8080/*).
# Falls der Key jemals im Klartext/Repo lag: rotieren und alten Key löschen.
VITE_GOOGLE_MAPS_API_KEY=
